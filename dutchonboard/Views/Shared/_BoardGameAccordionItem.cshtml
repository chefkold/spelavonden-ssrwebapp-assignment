@model BoardGame

<div class="accordion-item">
    <h2 class="accordion-header" id="flush-headingOne">
        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseOne" aria-expanded="false" aria-controls="flush-collapseOne">
            @Model.Name
        </button>
    </h2>
    <div id="flush-collapseOne" class="accordion-collapse collapse" aria-labelledby="flush-headingOne" data-bs-parent="#accordionFlushExample">
        <div class="accordion-body">
            <div class="w-100 text-center">
                <img src="data:@Model.ImageFormat;base64,@Convert.ToBase64String(Model.Image)" style="object-fit: cover" width="150px" />
            </div>
            <a asp-controller="BoardGame" asp-action="BoardGameDetailPage" asp-route-id="@Model.Id" class="btn btn-primary btn-primary-color mt-3 w-100">Bekijk</a>
        </div>
    </div>
</div>